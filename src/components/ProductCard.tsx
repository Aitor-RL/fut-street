import Link from "next/link";
import type { Product } from "@/data/products";
export function ProductCard({ product }: { product: Product }) { return <Link href={`/producto/${product.slug}`} className="product-card"><div className={`product-art art-${product.accent}`}>{product.badge && <span className="product-badge">{product.badge}</span>}<span className="art-number">0{product.id}</span><span className="art-label">{product.imageLabel}</span><span className="art-ball">◉</span></div><div className="product-meta"><div><h3>{product.name}</h3><p>{product.category}</p></div><strong>{product.price.toFixed(2).replace(".", ",")} €</strong></div></Link>; }
