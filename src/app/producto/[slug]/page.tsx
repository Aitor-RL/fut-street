import Link from "next/link";
import { notFound } from "next/navigation";
import { Footer } from "@/components/Footer";
import { Navbar } from "@/components/Navbar";
import { WhatsAppButton } from "@/components/WhatsAppButton";
import { getProduct, products } from "@/data/products";
export function generateStaticParams() { return products.map((product) => ({ slug: product.slug })); }
export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const product = getProduct(slug); if (!product) notFound(); return <><Navbar /><main className="product-page shell"><Link className="back-link" href="/catalogo">← Volver al catálogo</Link><div className="product-detail"><div className={`product-art detail-art art-${product.accent}`}><span className="art-number">0{product.id}</span><span className="art-label">{product.imageLabel}</span><span className="art-ball">◉</span></div><div className="product-info"><p className="eyebrow">{product.category} · {product.team}</p><h1>{product.name}</h1><p className="detail-price">{product.price.toFixed(2).replace(".", ",")} €</p><p className="detail-description">{product.description}</p><div className="size-block"><div><span className="eyebrow">Selecciona tu talla</span><Link href="/tallas">Guía de tallas</Link></div><div className="size-list">{product.sizes.map((size) => <span key={size}>{size}</span>)}</div></div><WhatsAppButton productName={product.name} /><p className="availability">Te confirmamos disponibilidad y condiciones de envío por WhatsApp.</p></div></div></main><Footer /></>; }
